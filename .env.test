# Test-run default (vitest runs with mode "test"). Mirrors .env.development
# so existing tests keep asserting against a real value; production never
# loads this file.
VITE_API_BASE_URL=http://localhost:3001
