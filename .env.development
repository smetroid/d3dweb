# Dev-only default. Production sets VITE_API_BASE_URL in the Vercel project
# settings to https://api.incisiveera.com; this file's value is never loaded
# outside `vite`/`vite dev`, so it can never leak into a production bundle.
VITE_API_BASE_URL=http://localhost:3001
